extends layout_adm

block content
  div(class="hero-image")
    div(class="hero-content")
      div(class="hero-content-inner")
        h1 Welcome to Motospot
        p(id="hometext") Easily find information about your favorite bike.

        button(type="button" onclick="window.location.href='/admMotospot/search'") Find a model